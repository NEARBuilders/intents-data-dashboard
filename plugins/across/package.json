{
  "name": "@data-provider/across",
  "version": "1.0.0",
  "description": "Across Protocol data provider plugin",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "author": "0xJesus",
  "keywords": ["across", "bridge", "defi", "near-intents", "data-provider", "every-plugin"],
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@effect/language-service": "^0.41.1",
    "@module-federation/node": "^2.7.15",
    "@rspack/cli": "^1.6.6",
    "@rspack/core": "^1.6.6",
    "@types/node": "^24.10.1",
    "dotenv": "^17.2.3",
    "every-plugin": "catalog:",
    "vite-tsconfig-paths": "^5.1.4",
    "vitest": "^3.2.4",
    "zephyr-rspack-plugin": "^0.1.2"
  },
  "dependencies": {
    "@data-provider/plugin-utils": "workspace:*",
    "@defuse-protocol/crosschain-assetid": "^1.0.2"
  },
  "scripts": {
    "build": "rspack build",
    "typecheck": "tsc -p tsconfig.build.json --noEmit",
    "dev": "rspack serve",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  }
}
